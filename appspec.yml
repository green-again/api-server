version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/green-again
hooks:
  BeforeInstall:
    - location: scripts/before_deploy.sh
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: scripts/after_deploy.sh
      timeout: 300
      runas: ubuntu
